services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: habit_user
      POSTGRES_PASSWORD: habit_password
      POSTGRES_DB: habit_tracker
    ports:
      - "5433:5432"
    volumes:
      - habit_db_data:/var/lib/postgresql/data

volumes:
  habit_db_data: